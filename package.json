{
  "name": "cypress-casino",
  "version": "1.0.0",
  "description": "Cypress project to test for demo.casino site",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "testSmoke": "npx cypress run --env grepTags=@smoke"
  },
  "author": "Julian Rodriguez",
  "license": "ISC",
  "devDependencies": {
    "@cypress/grep": "^3.1.5",
    "@cypress/xpath": "^2.0.3",
    "cypress": "^12.12.0",
    "cypress-mochawesome-reporter": "^3.4.0"
  }
}
